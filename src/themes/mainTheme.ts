import { createTheme } from "@mui/material";

const mainTheme = createTheme({
    palette: {
      primary: {
        main: "#FF6833",
      },
      secondary: {
        main: "#4169E1",
        dark: "#0B0E2C",
      },
    },
  });

export default mainTheme